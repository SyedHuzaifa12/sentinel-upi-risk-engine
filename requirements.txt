# --- Web application (Django) ---
asgiref==3.5.2
certifi==2022.9.24
cffi==2.1.1
charset-normalizer==2.1.1
cryptography==38.0.1
defusedxml==0.7.1
Django==4.1.2
idna==3.4
oauthlib==3.2.2
Pillow==9.2.0
pycparser==2.21
PyJWT==2.6.0
python-dotenv==0.19.0
python3-openid==3.2.0
pytz==2022.5
requests==2.28.1
requests-oauthlib==1.3.1
social-auth-app-django==5.0.0
social-auth-core==4.3.0
sqlparse==0.4.3
urllib3==1.26.12

# --- Production server / static files ---
gunicorn==21.2.0
whitenoise==6.6.0

# --- ML / fraud-prediction inference ---
# Required at runtime: the Django app unpickles a scikit-learn model via
# joblib, so scikit-learn must be installed even though the view code never
# imports it directly.
numpy==1.23.4
pandas==1.5.1
scikit-learn==1.1.3
joblib==1.2.0

# --- Notebooks (ml/notebooks/) ---
# Only needed to re-run the training/EDA notebooks, not for serving the app.
matplotlib==3.6.2
seaborn==0.12.1
# ydata-profiling is used by ml/notebooks/Classification Report.ipynb only;
# install separately if you need to regenerate that report (heavy transitive deps).

# --- Synthetic UPI event generator (ml/src/generator/) ---
pydantic==2.9.2
pytest==7.4.3

# --- feature_lib (event-driven risk engine) ---
psycopg[binary]==3.3.4
redis==8.1.0

# --- Phase 2 training (ml/src/train.py): cold/warm LightGBM models ---
lightgbm==4.7.0

# --- Phase 3 policy (ml/src/policy/reason_codes.py): SHAP explanations ---
# shap's own metadata declares numpy>=2, which conflicts with numpy==1.23.4
# pinned above for pandas 1.5.1 compatibility (pandas 1.5.1's compiled
# extensions are ABI-incompatible with numpy 2.x and crash on import).
# Verified working in practice: shap 0.51.0 runs fine against numpy 1.23.4
# at runtime; the >=2 constraint is not enforced by pip (already-installed
# numpy isn't downgraded by installing shap alone) and no numpy-2-only API
# is exercised by TreeExplainer's LightGBM path. Do not let pip "fix" this by
# upgrading numpy. Separately (unrelated to shap): `import lightgbm` must
# happen before `import pandas` in any process that also touches LightGBM's
# C API, or it crashes with an access violation -- see the comment in
# reason_codes.py.
shap==0.51.0
